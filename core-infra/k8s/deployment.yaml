apiVersion: apps/v1
kind: Deployment
metadata:
  name: aether-core
  namespace: aether-core
  labels:
    app: aether-core
    app.kubernetes.io/name: aether-core
    app.kubernetes.io/part-of: aether
spec:
  replicas: 2
  selector:
    matchLabels:
      app: aether-core
  template:
    metadata:
      labels:
        app: aether-core
        app.kubernetes.io/name: aether-core
    spec:
      serviceAccountName: aether-core
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: aether-core
          image: ghcr.io/suplab/aether-core:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8082
              protocol: TCP
          env:
            - name: POSTGRES_URL
              valueFrom:
                secretKeyRef:
                  name: aether-core-secrets
                  key: postgres-url
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: aether-core-secrets
                  key: postgres-user
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: aether-core-secrets
                  key: postgres-password
            - name: OLLAMA_BASE_URL
              valueFrom:
                configMapKeyRef:
                  name: aether-core-config
                  key: ollama-base-url
            - name: KAFKA_ENABLED
              value: "true"
            - name: KAFKA_BOOTSTRAP_SERVERS
              valueFrom:
                configMapKeyRef:
                  name: aether-core-config
                  key: kafka-bootstrap-servers
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          startupProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8082
            failureThreshold: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8082
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8082
            initialDelaySeconds: 20
            periodSeconds: 10
            failureThreshold: 3
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
